### TO INTIALIZE TYPESCRIPT IN A PROJECT
```
tsc init (this is to initialize typescript configurtaion file tsconfig.json)
tsc (to transpile TS to JS)
ts-node (to run TS without transpiling, it's a node version in TS)
```
